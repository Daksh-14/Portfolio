const Footer = () => {
  return (
    <footer className="bg-gray-900 text-center py-4 text-gray-300">
      <p>© {new Date().getFullYear()} Daksh Valecha. Built with React + Tailwind CSS.</p>
    </footer>
  );
};

export default Footer;
